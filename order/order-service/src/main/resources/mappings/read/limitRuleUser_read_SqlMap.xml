<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.order.dao.read.LimitRuleUserReadDAO">
	<resultMap type="com.egeo.components.order.po.LimitRuleUserPO" id="limitRuleUserMap">
		<result property="id" column="id" />
		<result property="limitRuleId" column="limit_rule_id" />
		<result property="companyId" column="company_id" />
	</resultMap>

	<sql id="limitRuleUserColumns">
		id,
		limit_rule_id,
		company_id
	</sql>

	<select id="findById" parameterType="com.egeo.components.order.po.LimitRuleUserPO" resultMap="limitRuleUserMap">
		SELECT
		<include refid="limitRuleUserColumns" />
		FROM limit_rule_user
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="limitRuleUserMap">
		SELECT
		<include refid="limitRuleUserColumns" />
		FROM limit_rule_user
		<where>
			<if test="po.limitRuleId != null">
				and limit_rule_id = #{po.limitRuleId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM limit_rule_user
		<where>
			<if test="po.limitRuleId != null">
				and limit_rule_id = #{po.limitRuleId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="limitRuleUserMap">
		SELECT
		<include refid="limitRuleUserColumns" />
		FROM limit_rule_user
		<where>
			<if test="po.limitRuleId != null">
				and limit_rule_id = #{po.limitRuleId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>



	<select id="findLimitRuleAllByParam"  resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM limit_rule_user
		<where>
			<if test="po.limitRuleId != null">
				and limit_rule_id = #{po.limitRuleId}
			</if>
			<if test="po.companyId != null">
				and (company_id = #{po.companyId} OR company_id= -1)
			</if>
		</where>
	</select>
</mapper>
	