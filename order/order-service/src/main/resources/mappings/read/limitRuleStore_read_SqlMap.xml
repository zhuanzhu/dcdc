<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.order.dao.read.LimitRuleStoreReadDAO">
	<resultMap type="com.egeo.components.order.po.LimitRuleStorePO" id="limitRuleStoreMap">
		<result property="id" column="id" />
		<result property="limitRuleId" column="limit_rule_id" />
		<result property="storeId" column="store_id" />
	</resultMap>

	<sql id="limitRuleStoreColumns">
		id,
		limit_rule_id,
		store_id
	</sql>

	<select id="findById" parameterType="com.egeo.components.order.po.LimitRuleStorePO" resultMap="limitRuleStoreMap">
		SELECT
		<include refid="limitRuleStoreColumns" />
		FROM limit_rule_store
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="limitRuleStoreMap">
		SELECT
		<include refid="limitRuleStoreColumns" />
		FROM limit_rule_store
		<where>
			<if test="po.limitRuleId != null">
				and limit_rule_id = #{po.limitRuleId}
			</if>
			<if test="po.storeId != null">
				and store_id = #{po.storeId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM limit_rule_store
		<where>
			<if test="po.limitRuleId != null">
				and limit_rule_id = #{po.limitRuleId}
			</if>
			<if test="po.storeId != null">
				and store_id = #{po.storeId}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="limitRuleStoreMap">
		SELECT
		<include refid="limitRuleStoreColumns" />
		FROM limit_rule_store
		<where>
			<if test="po.limitRuleId != null">
				and limit_rule_id = #{po.limitRuleId}
			</if>
			<if test="po.storeId != null ">
				and (store_id = #{po.storeId} OR store_id=-1)
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>

	<select id="findLimitRuleStoreCount" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM limit_rule_store
		<where>
			<if test="po.limitRuleId != null">
				and limit_rule_id = #{po.limitRuleId}
			</if>
			<if test="po.storeId != null">
				and (store_id = #{po.storeId} OR store_id=-1)
			</if>
		</where>
	</select>
</mapper>
	