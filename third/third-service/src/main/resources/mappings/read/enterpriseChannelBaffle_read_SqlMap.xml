<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.third.dao.read.EnterpriseChannelBaffleReadDAO">

	<resultMap type="com.egeo.components.third.po.EnterpriseChannelBafflePO" id="enterpriseChannelBaffleMap">
		<result property="id" column="id" />
		<result property="enterpriseId" column="enterprise_id" />
		<result property="channelCode" column="channel_code" />
		<result property="channelServiceName" column="channel_service_name" />
		<result property="channelServiceType" column="channel_service_type" />
		<result property="ifDecrypt" column="if_decrypt" />
		<result property="returnData" column="return_data" />
		<result property="state" column="state" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time" />
	</resultMap>

	<sql id="enterpriseChannelBaffleColumns">
		id,
		enterprise_id,
		channel_code,
		channel_service_name,
		channel_service_type,
		state,
		return_data,
		if_decrypt,
		create_time,
		update_time
	</sql>


	<sql id="whereSql">
		<where>
			<if test="po.id != null">
				and id =  #{po.id}
			</if>
			<if test="po.enterpriseId != null">
				and enterprise_id = #{po.enterpriseId}
			</if>
			<if test="po.channelCode != null">
				and channel_code = #{po.channelCode}
			</if>
			<if test="po.channelServiceName != null">
				and channel_service_name = #{po.channelServiceName}
			</if>
			<if test="po.channelServiceType != null">
				and channel_service_type = #{po.channelServiceType}
			</if>
			<if test="po.state != null">
				and state = #{po.state}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
	</sql>

	<sql id="inClause">
		in
		<foreach collection="ids" item="id" open="(" separator=","
				 close=")">
			#{id}
		</foreach>
	</sql>

	<sql id="tableNameId">
		enterprise_channel_baffle
	</sql>

<!--		<select id="findByIds" resultMap="enterpriseBizConfigMap">-->
<!--			select -->
<!--			       * -->
<!--			from -->
<!--				<include refid="tableNameId" />-->
<!--			where id -->
<!--			<include refid="inClause" />-->
<!--		</select>-->

	<select id="findById" parameterType="com.egeo.components.third.po.ChannelServiceConfigPO" resultMap="enterpriseChannelBaffleMap">
		SELECT
		<include refid="enterpriseChannelBaffleColumns" />
		FROM
		<include refid="tableNameId" />
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="enterpriseChannelBaffleMap">
		SELECT
			<include refid="enterpriseChannelBaffleColumns" />
		FROM
			<include refid="tableNameId" />
			<include refid="whereSql"/>
			<choose>
				<when test="page !=null and page.orderBy != null and page.orderBy != ''">
					ORDER BY ${page.orderBy}
				</when>
				<otherwise>
					ORDER BY id desc
				</otherwise>
			</choose>
			<if test="page != null">
			limit #{page.limitStart},#{page.pageSize}
			</if>
	</select>

	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
			count(1) cnt
		FROM
			<include refid="tableNameId" />
			<include refid="whereSql"/>
	</select>

	<select id="findAll" resultMap="enterpriseChannelBaffleMap">
		SELECT
		<include refid="enterpriseChannelBaffleColumns" />
		FROM
		<include refid="tableNameId" />
		<include refid="whereSql"/>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY id desc
			</otherwise>
		</choose>
	</select>
</mapper>
