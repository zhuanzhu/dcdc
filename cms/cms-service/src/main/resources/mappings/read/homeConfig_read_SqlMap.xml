<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.cms.dao.read.HomeConfigReadDAO">



	<resultMap type="com.egeo.components.cms.po.HomeConfigPO" id="homeConfigMap">
		<result property="id" column="id" />
		<result property="name" column="name" />
		<result property="display" column="display" />
		<result property="config" column="config" />
	</resultMap>

	<sql id="homeConfigColumns">
		id,
		name,
		display,
		config
	</sql>

	<select id="findById" parameterType="com.egeo.components.cms.po.HomeConfigPO" resultMap="homeConfigMap">
		SELECT
		<include refid="homeConfigColumns" />
		FROM home_config
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="homeConfigMap">
		SELECT
		<include refid="homeConfigColumns" />
		FROM home_config
		<where>
			<if test="po.name != null">
				and name like concat('%',#{po.name},'%')
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM home_config
		<where>
			<if test="po.name != null">
				and name like concat('%',#{po.name},'%')
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="homeConfigMap">
		SELECT
		<include refid="homeConfigColumns" />
		FROM home_config
		<where>
			<if test="po.name != null">
				and name like concat('%',#{po.name},'%')
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	