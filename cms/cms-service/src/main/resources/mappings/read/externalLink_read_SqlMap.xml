<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.cms.dao.read.ExternalLinkReadDAO">
	<resultMap type="com.egeo.components.cms.po.ExternalLinkPO" id="externalLinkMap">
		<result property="id" column="id" />
		<result property="name" column="name" />
		<result property="externalLinkUrl" column="external_link_url" />
		<result property="linkType" column="link_type" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time" />
	</resultMap>

	<sql id="externalLinkColumns">
		id,
		name,
		external_link_url,
		link_type,
		create_time,
		update_time
	</sql>

	<select id="findById" parameterType="com.egeo.components.cms.po.ExternalLinkPO" resultMap="externalLinkMap">
		SELECT
		<include refid="externalLinkColumns" />
		FROM external_link
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="externalLinkMap">
		SELECT
		<include refid="externalLinkColumns" />
		FROM external_link
		<where>
			<if test="po.name != null">
				and name = #{po.name}
			</if>
			<if test="po.externalLinkUrl != null">
				and external_link_url = #{po.externalLinkUrl}
			</if>
			<if test="po.linkType != null">
				and link_type = #{po.linkType}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM external_link
		<where>
			<if test="po.name != null">
				and name = #{po.name}
			</if>
			<if test="po.externalLinkUrl != null">
				and external_link_url = #{po.externalLinkUrl}
			</if>
			<if test="po.linkType != null">
				and link_type = #{po.linkType}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="externalLinkMap">
		SELECT
		<include refid="externalLinkColumns" />
		FROM external_link
		<where>
			<if test="po.name != null">
				and name = #{po.name}
			</if>
			<if test="po.externalLinkUrl != null">
				and external_link_url = #{po.externalLinkUrl}
			</if>
			<if test="po.linkType != null">
				and link_type = #{po.linkType}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	