<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.cms.dao.read.BannerInstReadDAO">
	<resultMap type="com.egeo.components.cms.po.BannerInstPO" id="bannerInstMap">
		<result property="id" column="id" />
		<result property="bannerId" column="banner_id" />
		<result property="instId" column="inst_id" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time" />
	</resultMap>

	<sql id="bannerInstColumns">
		id,
		banner_id,
		inst_id,
		create_time,
		update_time
	</sql>

	<select id="findById" parameterType="com.egeo.components.cms.po.BannerInstPO" resultMap="bannerInstMap">
		SELECT
		<include refid="bannerInstColumns" />
		FROM banner_inst
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="bannerInstMap">
		SELECT
		<include refid="bannerInstColumns" />
		FROM banner_inst
		<where>
			<if test="po.bannerId != null">
				and banner_id = #{po.bannerId}
			</if>
			<if test="po.instId != null">
				and inst_id = #{po.instId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM banner_inst
		<where>
			<if test="po.bannerId != null">
				and banner_id = #{po.bannerId}
			</if>
			<if test="po.instId != null">
				and inst_id = #{po.instId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="bannerInstMap">
		SELECT
		<include refid="bannerInstColumns" />
		FROM banner_inst
		<where>
			<if test="po.bannerId != null">
				and banner_id = #{po.bannerId}
			</if>
			<if test="po.instId != null">
				and inst_id = #{po.instId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	