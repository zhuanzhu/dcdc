<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.write.StandardUnitTagWriteDAO">
	
	<sql id="inClause">
		not in
		<foreach collection="ids" item="id" open="(" separator="," close=")">
			#{id}
		</foreach>
	</sql>
	
	<sql id="insertAllClause">
		<foreach collection="poList" item="po" separator=",">
				(#{po.standardUnitId},
				#{po.tagId})
		</foreach>
	</sql>
	
	<insert id="insert" parameterType="com.egeo.components.product.po.StandardUnitTagPO">
		INSERT INTO standard_unit_tag(
        <trim suffixOverrides=",">
			<if test="standardUnitId != null">
				standard_unit_id,
			</if>
			<if test="tagId != null">
				tag_id,
			</if>
        </trim>		
		) VALUES (
        <trim suffixOverrides=",">
			<if test="standardUnitId != null">
				#{standardUnitId},
			</if>
			<if test="tagId != null">
				#{tagId},
			</if>
        </trim>		
		)
		<selectKey keyProperty="id" resultType="java.lang.Long">
			select LAST_INSERT_ID()
		</selectKey>
	</insert>
	
	<insert id="insertAll">
		INSERT INTO standard_unit_tag(
				standard_unit_id,
				tag_id
		) VALUES <include refid="insertAllClause" /> 
	</insert>


	
	
	<update id="update">
	UPDATE standard_unit_tag 
		<set>
			<if test="standardUnitId != null">
				standard_unit_id = #{standardUnitId},
			</if>			
			<if test="tagId != null">
				tag_id = #{tagId},
			</if>			
		</set>
			WHERE id = #{id}
	</update>	
	

	<delete id="delete">
		delete from standard_unit_tag where id = #{id}
	</delete>

	<delete id="deleteByPara">
		delete from standard_unit_tag 
		<where>
			<if test="standardUnitId != null">
				and standard_unit_id = #{standardUnitId}
			</if>
			<if test="tagId != null">
				and tag_id = #{tagId}
			</if>
		</where>
	</delete>
	
	<delete id="delByTags">
		delete from standard_unit_tag where standard_unit_id = #{standardUnitId} and tag_id <include refid="inClause" />
	</delete>


</mapper>
	