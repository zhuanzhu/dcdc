<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.read.CategoryTagReadDAO">
	<resultMap type="com.egeo.components.product.po.CategoryTagPO" id="categoryTagMap">
		<result property="id" column="id" />
		<result property="categoryId" column="category_id" />
		<result property="tagId" column="tag_id" />
	</resultMap>

	<sql id="categoryTagColumns">
		id,
		category_id,
		tag_id
	</sql>

	<select id="findById" parameterType="com.egeo.components.product.po.CategoryTagPO" resultMap="categoryTagMap">
		SELECT
		<include refid="categoryTagColumns" />
		FROM category_tag
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="categoryTagMap">
		SELECT
		<include refid="categoryTagColumns" />
		FROM category_tag
		<where>
			<if test="po.categoryId != null">
				and category_id = #{po.categoryId}
			</if>
			<if test="po.tagId != null">
				and tag_id = #{po.tagId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM category_tag
		<where>
			<if test="po.categoryId != null">
				and category_id = #{po.categoryId}
			</if>
			<if test="po.tagId != null">
				and tag_id = #{po.tagId}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="categoryTagMap">
		SELECT
		<include refid="categoryTagColumns" />
		FROM category_tag
		<where>
			<if test="po.categoryId != null">
				and category_id = #{po.categoryId}
			</if>
			<if test="po.tagId != null">
				and tag_id = #{po.tagId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id
			</otherwise>
		</choose>
	</select>
</mapper>
	