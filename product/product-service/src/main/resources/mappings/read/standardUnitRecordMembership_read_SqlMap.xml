<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.read.StandardUnitRecordMembershipReadDAO">
	<resultMap type="com.egeo.components.product.po.StandardUnitRecordMembershipPO" id="standardUnitRecordMembershipMap">
		<result property="id" column="id" />
		<result property="membershipId" column="membership_id" />
		<result property="standardUnitRecordId" column="standard_unit_record_id" />
		<result property="platformId" column="platform_id" />
	</resultMap>

	<sql id="standardUnitRecordMembershipColumns">
		id,
		membership_id,
		standard_unit_record_id,
		platform_id
	</sql>

	<select id="findById" parameterType="com.egeo.components.product.po.StandardUnitRecordMembershipPO" resultMap="standardUnitRecordMembershipMap">
		SELECT
		<include refid="standardUnitRecordMembershipColumns" />
		FROM standard_unit_record_membership
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="standardUnitRecordMembershipMap">
		SELECT
		<include refid="standardUnitRecordMembershipColumns" />
		FROM standard_unit_record_membership
		<where>
			<if test="po.membershipId != null">
				and membership_id = #{po.membershipId}
			</if>
			<if test="po.standardUnitRecordId != null">
				and standard_unit_record_id = #{po.standardUnitRecordId}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM standard_unit_record_membership
		<where>
			<if test="po.membershipId != null">
				and membership_id = #{po.membershipId}
			</if>
			<if test="po.standardUnitRecordId != null">
				and standard_unit_record_id = #{po.standardUnitRecordId}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="standardUnitRecordMembershipMap">
		SELECT
		<include refid="standardUnitRecordMembershipColumns" />
		FROM standard_unit_record_membership
		<where>
			<if test="po.membershipId != null">
				and membership_id = #{po.membershipId}
			</if>
			<if test="po.standardUnitRecordId != null">
				and standard_unit_record_id = #{po.standardUnitRecordId}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	