<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.read.MerchantProdClientReadDAO">
	<resultMap type="com.egeo.components.product.po.MerchantProdClientPO" id="merchantProdClientMap">
		<result property="id" column="id" />
		<result property="merchantProductId" column="merchant_product_id" />
		<result property="clientId" column="client_id" />
		<result property="createTime" column="create_time" />
	</resultMap>

	<sql id="merchantProdClientColumns">
		id,
		merchant_product_id,
		client_id,
		create_time
	</sql>

	<select id="findById" parameterType="com.egeo.components.product.po.MerchantProdClientPO" resultMap="merchantProdClientMap">
		SELECT
		<include refid="merchantProdClientColumns" />
		FROM merchant_prod_client
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="merchantProdClientMap">
		SELECT
		<include refid="merchantProdClientColumns" />
		FROM merchant_prod_client
		<where>
			<if test="po.merchantProductId != null">
				and merchant_product_id = #{po.merchantProductId}
			</if>
			<if test="po.clientId != null">
				and client_id = #{po.clientId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM merchant_prod_client
		<where>
			<if test="po.merchantProductId != null">
				and merchant_product_id = #{po.merchantProductId}
			</if>
			<if test="po.clientId != null">
				and client_id = #{po.clientId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="merchantProdClientMap">
		SELECT
		<include refid="merchantProdClientColumns" />
		FROM merchant_prod_client
		<where>
			<if test="po.merchantProductId != null">
				and merchant_product_id = #{po.merchantProductId}
			</if>
			<if test="po.clientId != null">
				and client_id = #{po.clientId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	