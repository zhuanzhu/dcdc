<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.read.MerchantProductCompanyReadDAO">
	<resultMap type="com.egeo.components.product.po.MerchantProductCompanyPO" id="merchantProductCompanyMap">
		<result property="id" column="id" />
		<result property="merchantProductId" column="merchant_product_id" />
		<result property="companyId" column="company_id" />
		<result property="createTime" column="create_time" />
		<result property="companyType" column="company_type" />
	</resultMap>

	<sql id="merchantProductCompanyColumns">
		id,
		merchant_product_id,
		company_id,
		create_time,
		company_type
	</sql>

	<select id="findById" parameterType="com.egeo.components.product.po.MerchantProductCompanyPO" resultMap="merchantProductCompanyMap">
		SELECT
		<include refid="merchantProductCompanyColumns" />
		FROM merchant_product_company
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="merchantProductCompanyMap">
		SELECT
		<include refid="merchantProductCompanyColumns" />
		FROM merchant_product_company
		<where>
			<if test="po.merchantProductId != null">
				and merchant_product_id = #{po.merchantProductId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM merchant_product_company
		<where>
			<if test="po.merchantProductId != null">
				and merchant_product_id = #{po.merchantProductId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="merchantProductCompanyMap">
		SELECT
		<include refid="merchantProductCompanyColumns" />
		FROM merchant_product_company
		<where>
			<if test="po.merchantProductId != null">
				and merchant_product_id = #{po.merchantProductId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	