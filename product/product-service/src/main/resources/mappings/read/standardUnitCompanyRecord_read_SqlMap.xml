<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.read.StandardUnitCompanyRecordReadDAO">
	<resultMap type="com.egeo.components.product.po.StandardUnitCompanyRecordPO" id="standardUnitCompanyRecordMap">
		<result property="id" column="id" />
		<result property="standardUnitRecordId" column="standard_unit_record_id" />
		<result property="companyId" column="company_id" />
		<result property="createTime" column="create_time" />
	</resultMap>

	<sql id="standardUnitCompanyRecordColumns">
		id,
		standard_unit_record_id,
		company_id,
		create_time
	</sql>

	<select id="findById" parameterType="com.egeo.components.product.po.StandardUnitCompanyRecordPO" resultMap="standardUnitCompanyRecordMap">
		SELECT
		<include refid="standardUnitCompanyRecordColumns" />
		FROM standard_unit_company_record
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="standardUnitCompanyRecordMap">
		SELECT
		<include refid="standardUnitCompanyRecordColumns" />
		FROM standard_unit_company_record
		<where>
			<if test="po.standardUnitRecordId != null">
				and standard_unit_record_id = #{po.standardUnitRecordId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM standard_unit_company_record
		<where>
			<if test="po.standardUnitRecordId != null">
				and standard_unit_record_id = #{po.standardUnitRecordId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="standardUnitCompanyRecordMap">
		SELECT
		<include refid="standardUnitCompanyRecordColumns" />
		FROM standard_unit_company_record
		<where>
			<if test="po.standardUnitRecordId != null">
				and standard_unit_record_id = #{po.standardUnitRecordId}
			</if>
			<if test="po.companyId != null">
				and company_id = #{po.companyId}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	