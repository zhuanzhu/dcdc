<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.read.ChannelCategoryReadDAO">

	<resultMap type="com.egeo.components.product.po.ChannelCategoryPO" id="channelCategoryMap">
		<result property="id" column="id" />
		<result property="channelCode" column="channel_code" />
		<result property="channelId" column="channel_id" />
		<result property="channelCategoryName" column="channel_category_name" />
		<result property="parentId" column="parent_id" />
		<result property="innerCategoryId" column="inner_category_id" />
		<result property="channelCategoryLevel" column="channel_category_level" />
		<result property="channelCategoryType" column="channel_category_type" />
		<result property="channelCategoryId" column="channel_category_id" />
		<result property="channelCategoryPId" column="channel_category_p_id" />
		<result property="remark" column="remark" />
	</resultMap>

	<sql id="channelCategoryColumns">
		id,
		channel_id,
		channel_code,
		channel_category_name,
		parent_id,
		inner_category_id,
		channel_category_level,
		channel_category_type,
		channel_category_id,
		channel_category_p_id,
		remark
	</sql>


	<sql id="whereSql">
		<where>
			<if test="po.id != null">
				and id =  #{po.id}
			</if>
			<if test="po.channelCode != null">
				and channel_code = #{po.channelCode}
			</if>
			<if test="po.channelCategoryLevel != null">
				and channel_category_level = #{po.channelCategoryLevel}
			</if>
			<if test="po.channelCategoryPId != null">
				and channel_category_p_id = #{po.channelCategoryPId}
			</if>
			<if test="po.parentId != null">
				and parent_id = #{po.parentId}
			</if>
			<if test="po.channelCategoryId != null">
				and channel_category_id = #{po.channelCategoryId}
			</if>
		</where>
	</sql>

	<sql id="inClause">
		in
		<foreach collection="ids" item="id" open="(" separator=","
				 close=")">
			#{id}
		</foreach>
	</sql>

	<sql id="tableNameId">
		channel_category
	</sql>

<!--		<select id="findByIds" resultMap="channelServiceConfigMap">-->
<!--			select * from channel_service_config-->
<!--			where id -->
<!--			<include refid="inClause" />-->
<!--		</select>-->

	<select id="findById" parameterType="com.egeo.components.product.po.ChannelProductTextPO" resultMap="channelCategoryMap">
		SELECT
		<include refid="channelCategoryColumns" />
		FROM
		<include refid="tableNameId" />
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="channelCategoryMap">
		SELECT
			<include refid="channelCategoryColumns" />
		FROM
			<include refid="tableNameId" />
			<include refid="whereSql"/>
			<choose>
				<when test="page !=null and page.orderBy != null and page.orderBy != ''">
					ORDER BY ${page.orderBy}
				</when>
				<otherwise>
					ORDER BY id desc
				</otherwise>
			</choose>
			<if test="page != null">
			limit #{page.limitStart},#{page.pageSize}
			</if>
	</select>

	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
			count(1) cnt
		FROM
			<include refid="tableNameId" />
			<include refid="whereSql"/>
	</select>

	<select id="findAll" resultMap="channelCategoryMap">
		SELECT
		<include refid="channelCategoryColumns" />
		FROM
		<include refid="tableNameId" />
		<include refid="whereSql"/>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY id desc
			</otherwise>
		</choose>
	</select>
</mapper>
