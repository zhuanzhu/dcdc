<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.read.StoreMenuNodeStandardUnitReadDAO">
	<resultMap type="com.egeo.components.product.po.StoreMenuNodeStandardUnitPO" id="storeMenuNodeStandardUnitMap">
		<result property="id" column="id" />
		<result property="storeMenuNodeId" column="store_menu_node_id" />
		<result property="standardUnitId" column="standard_unit_id" />
		<result property="platformId" column="platform_id" />
	</resultMap>

	<sql id="storeMenuNodeStandardUnitColumns">
		id,
		store_menu_node_id,
		standard_unit_id,
		platform_id
	</sql>

	<select id="findById" parameterType="com.egeo.components.product.po.StoreMenuNodeStandardUnitPO" resultMap="storeMenuNodeStandardUnitMap">
		SELECT
		<include refid="storeMenuNodeStandardUnitColumns" />
		FROM store_menu_node_standard_unit
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="storeMenuNodeStandardUnitMap">
		SELECT
		<include refid="storeMenuNodeStandardUnitColumns" />
		FROM store_menu_node_standard_unit
		<where>
			<if test="po.storeMenuNodeId != null">
				and store_menu_node_id = #{po.storeMenuNodeId}
			</if>
			<if test="po.standardUnitId != null">
				and standard_unit_id = #{po.standardUnitId}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM store_menu_node_standard_unit
		<where>
			<if test="po.storeMenuNodeId != null">
				and store_menu_node_id = #{po.storeMenuNodeId}
			</if>
			<if test="po.standardUnitId != null">
				and standard_unit_id = #{po.standardUnitId}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="storeMenuNodeStandardUnitMap">
		SELECT
		<include refid="storeMenuNodeStandardUnitColumns" />
		FROM store_menu_node_standard_unit
		<where>
			<if test="po.storeMenuNodeId != null">
				and store_menu_node_id = #{po.storeMenuNodeId}
			</if>
			<if test="po.standardUnitId != null">
				and standard_unit_id = #{po.standardUnitId}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		group by standard_unit_id
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	