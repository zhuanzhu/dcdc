<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.product.dao.read.TagReadDAO">
	<resultMap type="com.egeo.components.product.po.TagPO" id="tagMap">
		<result property="id" column="id" />
		<result property="name" column="name" />
		<result property="type" column="type" />
		<result property="isValid" column="is_valid" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time" />
		<result property="platformId" column="platform_id" />
	</resultMap>

	<sql id="tagColumns">
		id,
		name,
		type,
		is_valid,
		create_time,
		update_time,
		platform_id
	</sql>
	
	<sql id="inClause">
		in
		<foreach collection="ids" item="id" open="(" separator=","
			close=")">
			#{id}
		</foreach>
	</sql>

	<select id="findById" parameterType="com.egeo.components.product.po.TagPO" resultMap="tagMap">
		SELECT
		<include refid="tagColumns" />
		FROM tag
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="tagMap">
		SELECT
		<include refid="tagColumns" />
		FROM tag
		<where>
			<if test="po.name != null">
				and name like concat('%',#{po.name},'%')
			</if>
			<if test="po.type != null">
				and type = #{po.type}
			</if>
			<if test="po.isValid != null">
				and is_valid = #{po.isValid}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM tag
		<where>
			<if test="po.name != null">
				and name like concat('%',#{po.name},'%')
			</if>
			<if test="po.type != null">
				and type = #{po.type}
			</if>
			<if test="po.isValid != null">
				and is_valid = #{po.isValid}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="tagMap">
		SELECT
		<include refid="tagColumns" />
		FROM tag
		<where>
			<if test="po.name != null">
				and name = #{po.name}
			</if>
			<if test="po.type != null">
				and type = #{po.type}
			</if>
			<if test="po.isValid != null">
				and is_valid = #{po.isValid}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
	
	<select id="findTagOfPageBystandardUnitCId" resultMap="tagMap">
		SELECT
		<include refid="tagColumns" />
		FROM tag
		<where>
			<if test="ids != null">
				and id <include refid="inClause" />
			</if>
			<if test="po.name != null">
				and name like concat('%',#{po.name},'%')
			</if>
			<if test="po.type != null">
				and type = #{po.type}
			</if>
			<!-- 用ids查询types参数传递错误 -->
			<!-- <if test="types != null">
				and type  in
				<foreach collection="types" item="type" open="(" separator=","
						 close=")">
					#{type}
				</foreach>
			</if> -->
			<if test="po.isValid != null">
				and is_valid = #{po.isValid}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPageBystandardUnitCId" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM tag
		<where>
			<if test="ids != null">
				and id <include refid="inClause" />
			</if>
			<if test="po.name != null">
				and name like concat('%',#{po.name},'%')
			</if>
			<if test="po.type != null">
				and type = #{po.type}
			</if>
			<!-- 用ids查询types参数传递错误 -->
			<!-- <if test="types != null">
				and type  in
				<foreach collection="types" item="type" open="(" separator=","
						 close=")">
					#{type}
				</foreach>
			</if> -->
			<if test="po.isValid != null">
				and is_valid = #{po.isValid}
			</if>
			<if test="po.createTime != null">
				and create_time = #{po.createTime}
			</if>
			<if test="po.updateTime != null">
				and update_time = #{po.updateTime}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
	</select>
</mapper>
	