<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.user.dao.write.CompanyUserDisabledWriteDAO">
	<insert id="insert" parameterType="com.egeo.components.user.po.CompanyUserDisabledPO">
		INSERT INTO u_company_user_disabled(
        <trim suffixOverrides=",">
			<if test="companyId != null">
				company_id,
			</if>
			<if test="userId != null">
				user_id,
			</if>
			<if test="revalidation != null">
				#{revalidation},
			</if>
        </trim>
		) VALUES (
        <trim suffixOverrides=",">
			<if test="companyId != null">
				#{companyId},
			</if>
			<if test="userId != null">
				#{userId},
			</if>
			<if test="revalidation != null">
				#{revalidation},
			</if>
        </trim>		
		)
		<selectKey keyProperty="id" resultType="java.lang.Long">
			select LAST_INSERT_ID()
		</selectKey>
	</insert>


	
	
	<update id="update">
	UPDATE u_company_user_disabled 
		<set>
			<if test="companyId != null">
				company_id = #{companyId},
			</if>			
			<if test="userId != null">
				user_id = #{userId},
			</if>
			<if test="disabledTime != null">
				disabled_time = #{disabledTime},
			</if>
			<if test="revalidation != null">
				revalidation = #{revalidation},
			</if>
		</set>
			WHERE id = #{id}
	</update>	
	

	<delete id="delete">
		delete from u_company_user_disabled where id = #{id}
	</delete>

	<delete id="deleteByPara">
		delete from u_company_user_disabled 
		<where>
			<if test="companyId != null">
				and company_id = #{companyId}
			</if>
			<if test="userId != null">
				and user_id = #{userId}
			</if>
			<if test="disabledTime != null">
				and disabled_time = #{disabledTime}
			</if>

		</where>
	</delete>

	<update id="updateRevalidationByCompanyId">
		UPDATE u_company_user_disabled
		SET revalidation = #{revalidation}
		WHERE company_id = #{companyId}
	</update>

</mapper>
	