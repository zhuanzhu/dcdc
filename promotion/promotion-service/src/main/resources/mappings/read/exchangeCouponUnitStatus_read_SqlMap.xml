<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egeo.components.promotion.dao.read.ExchangeCouponUnitStatusReadDAO">
	<resultMap type="com.egeo.components.promotion.po.ExchangeCouponUnitStatusPO" id="exchangeCouponUnitStatusMap">
		<result property="id" column="id" />
		<result property="exchangeId" column="exchange_id" />
		<result property="allowExchangeUnitStatus" column="allow_exchange_unit_status" />
		<result property="platformId" column="platform_id" />
	</resultMap>

	<sql id="exchangeCouponUnitStatusColumns">
		id,
		exchange_id,
		allow_exchange_unit_status,
		platform_id
	</sql>

	<select id="findById" parameterType="com.egeo.components.promotion.po.ExchangeCouponUnitStatusPO" resultMap="exchangeCouponUnitStatusMap">
		SELECT
		<include refid="exchangeCouponUnitStatusColumns" />
		FROM exchange_coupon_unit_status
		WHERE id = #{po.id}
	</select>

	<select id="findOfPage" resultMap="exchangeCouponUnitStatusMap">
		SELECT
		<include refid="exchangeCouponUnitStatusColumns" />
		FROM exchange_coupon_unit_status
		<where>
			<if test="po.exchangeId != null">
				and exchange_id = #{po.exchangeId}
			</if>
			<if test="po.allowExchangeUnitStatus != null">
				and allow_exchange_unit_status = #{po.allowExchangeUnitStatus}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>		
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
		<if test="page != null">
		limit #{page.limitStart},#{page.pageSize}
		</if>
	</select>
	
	<select id="countOfPage" resultType="java.lang.Integer">
		SELECT
		count(1) cnt
		FROM exchange_coupon_unit_status
		<where>
			<if test="po.exchangeId != null">
				and exchange_id = #{po.exchangeId}
			</if>
			<if test="po.allowExchangeUnitStatus != null">
				and allow_exchange_unit_status = #{po.allowExchangeUnitStatus}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
	</select>
	
	<select id="findAll" resultMap="exchangeCouponUnitStatusMap">
		SELECT
		<include refid="exchangeCouponUnitStatusColumns" />
		FROM exchange_coupon_unit_status
		<where>
			<if test="po.exchangeId != null">
				and exchange_id = #{po.exchangeId}
			</if>
			<if test="po.allowExchangeUnitStatus != null">
				and allow_exchange_unit_status = #{po.allowExchangeUnitStatus}
			</if>
			<if test="po.platformId != null">
				and platform_id = #{po.platformId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>			
			<otherwise>
				ORDER BY id desc		
			</otherwise>
		</choose>
	</select>
</mapper>
	